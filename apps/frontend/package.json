{
  "name": "@sui-safety-middleware/frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo \"No frontend build yet\" && exit 0",
    "test": "echo \"No frontend tests yet\" && exit 0",
    "lint": "echo \"No frontend lint config yet\" && exit 0"
  }
}
