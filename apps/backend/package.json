{
  "name": "@sui-safety-middleware/backend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p ../../tsconfig.json --noEmit",
    "test": "node --test",
    "lint": "echo \"No backend lint config yet\" && exit 0"
  }
}
